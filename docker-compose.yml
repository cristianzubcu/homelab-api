services:
  homelab-api:
    build: .
    container_name: homelab-api
    ports:
      - "5000:5000"
    env_file: .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /opt/homelab-data/media:/data:ro
    restart: unless-stopped
